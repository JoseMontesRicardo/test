const host 		= `localhost`
const dataBase 	= `testdb`
const user      = `joseAdmin`
const pass      = `R1c@rd001`
const textPass  = `R1SAsass0/\/\P4$$w0rDaas%&3231`
const rounds    = 3

//admin: joseAdmin
//pass : R1c@rd001

const values = {
    apisecret 	: `R1SAsass0/\/\P4$$w0rDaas%&3231`,
	dburl 		: `mongodb://${host}/${dataBase}`,
    textPass    : textPass,
    rounds      : rounds
}

module.exports = values